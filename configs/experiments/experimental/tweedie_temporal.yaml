# Temporal proxy: Tweedie (count-aware GLM).
# EXPERIMENTAL: Not part of the active benchmark suite. Tweedie is a poor methodological fit for the shared target/preprocessing framework.
experiment_name: tweedie_temporal
task_type: regression
benchmark:
  benchmark_mode: temporal_proxy
  model_family: count_aware_glm
  is_diagnostic_model: false

data:
  processed_path: "data/processed/openalex_temporal_articles_1000.parquet"
  dataset_id: openalex_temporal_articles_1000

target:
  name: cited_by_count
  transform: log1p
  target_mode: proxy

features:
  numeric:
    - publication_year
    - referenced_works_count
    - authors_count
    - institutions_count
  categorical:
    - type
    - language
    - venue_name
    - primary_topic
    - open_access_is_oa

split:
  split_kind: time
  time_column: publication_year
  train_year_end: 2018
  test_year_start: 2019
  test_size: 0.2
  random_state: 42

model:
  name: tweedie
  params: {}

evaluation:
  metrics:
    - rmse
    - mae
    - r2
