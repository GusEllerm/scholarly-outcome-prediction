# Environment variables for scholarly-outcome-prediction

# Optional: used to identify you to the OpenAlex API polite pool.
OPENALEX_MAILTO=your.email@example.org

# Optional: base directory for data (defaults to ./data if unset)
# DATA_DIR=/absolute/path/to/data

